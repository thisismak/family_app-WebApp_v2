<!DOCTYPE html>
<html lang="zh-TW">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="/css/style.css" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/marked@4.0.12/marked.min.css" rel="stylesheet">
  <title>家庭筆記本</title>
  <style>
    .note-editor { height: 300px; }
    .note-preview { min-height: 300px; border: 1px solid var(--dark-pink); border-radius: 8px; padding: 1rem; background:#fff; }
    .note-item { cursor:pointer; }
    .note-item:hover { background:var(--light-pink); }
  </style>
</head>
<body>
  <div class="container mt-5 notebook">
    <h2 class="text-center mb-4">家庭筆記本</h2>

    <div class="row">
      <!-- 左側筆記列表 -->
      <div class="col-md-4">
        <button id="newNoteBtn" class="btn btn-primary w-100 mb-3">新增筆記</button>
        <ul id="noteList" class="list-group"></ul>
      </div>

      <!-- 右側編輯區 -->
      <div class="col-md-8">
        <form id="noteForm" class="card p-4 d-none">
          <input type="hidden" id="noteId">
          <div class="mb-3">
            <label class="form-label text-info">標題</label>
            <input type="text" id="noteTitle" class="form-control" required>
          </div>
          <div class="mb-3">
            <label class="form-label text-info">內容（支援 Markdown）</label>
            <textarea id="noteContent" class="form-control note-editor"></textarea>
          </div>
          <div class="row">
            <div class="col-6">
              <button type="button" id="saveNoteBtn" class="btn btn-primary w-100">儲存</button>
            </div>
            <div class="col-6">
              <button type="button" id="deleteNoteBtn" class="btn btn-danger w-100 d-none">刪除</button>
            </div>
          </div>
        </form>

        <div id="previewArea" class="note-preview d-none mt-3"></div>
      </div>
    </div>

    <div class="text-center mt-4">
      <a href="/dashboard" class="btn btn-secondary">返回儀表板</a>
    </div>
  </div>

  <!-- Markdown -->
  <script src="https://cdn.jsdelivr.net/npm/marked@4.0.12/marked.min.js"></script>
  <script src="/js/notebook.js" defer></script>
</body>
</html>