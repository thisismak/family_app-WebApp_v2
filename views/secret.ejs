<!DOCTYPE html>
<html lang="zh-TW">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta name="robots" content="noindex, nofollow">
  <title>願望隨機選擇器 ♥</title>
  <style>
    body { 
      background: linear-gradient(135deg, #ffebee, #fce4ec); 
      font-family: 20px 'Microsoft JhengHei', Arial, sans-serif; 
      text-align: center; 
      padding: 30px; 
      color: #c2185b;
      min-height: 100vh;
      margin: 0;
    }
    .container { 
      max-width: 800px; 
      margin: 40px auto; 
      background: white; 
      padding: 50px 40px; 
      border-radius: 30px; 
      box-shadow: 0 15px 40px rgba(233, 30, 99, 0.2);
    }
    h1 { 
      font-size: 48px; 
      color: #e91e63; 
      margin: 0 0 20px 0;
    }
    .heart { 
      color: #e91e63; 
      font-size: 70px; 
      animation: beat 1.5s infinite; 
      margin: 20px 0;
    }
    @keyframes beat {
      0%,100% { transform: scale(1); }
      50% { transform: scale(1.15); }
    }
    textarea {
      width: 100%;
      height: 200px;
      padding: 15px;
      border: 3px solid #f8bbd0;
      border-radius: 15px;
      font-size: 18px;
      margin: 20px 0;
      box-sizing: border-box;
      resize: vertical;
    }
    button {
      background: #e91e63;
      color: white;
      border: none;
      padding: 18px 50px;
      font-size: 24px;
      border-radius: 50px;
      cursor: pointer;
      box-shadow: 0 8px 20px rgba(233,30,99,0.4);
      transition: all 0.3s;
    }
    button:hover {
      background: #c2185b;
      transform: translateY(-3px);
    }
    #result {
      margin-top: 40px;
      font-size: 36px;
      font-weight: bold;
      min-height: 80px;
      opacity: 0;
      transition: all 0.8s;
    }
    .sparkle {
      font-size: 50px;
      opacity: 0;
    }
  </style>
</head>
<body>
  <div class="heart">♥ ♥ ♥</div>

  <div class="container">
    <h1>願望隨機選擇器</h1>
    <p style="font-size:24px;color:#e91e63;">把你的選項寫在下面，每行一個<br>然後讓命運幫你決定吧～</p>

    <textarea id="wishes" placeholder="例如：
今晚吃火鍋
今晚吃燒烤
今晚吃壽司
今晚吃披薩
叫外送躺平"></textarea>

    <button id="decideBtn">幫我決定！♥</button>

    <div id="result"></div>
    <div class="sparkle" id="sparkles"></div>
  </div>

  <div class="heart">♥ ♥ ♥</div>

  <script>
    const btn = document.getElementById('decideBtn');
    const textarea = document.getElementById('wishes');
    const resultDiv = document.getElementById('result');
    const sparkles = document.getElementById('sparkles');

    btn.addEventListener('click', () => {
      const text = textarea.value.trim();
      if (!text) {
        alert('請至少輸入一個願望喔～♥');
        return;
      }

      const wishes = text.split('\n')
                        .map(s => s.trim())
                        .filter(s => s.length > 0);

      if (wishes.length === 0) {
        alert('請至少輸入一個願望喔～♥');
        return;
      }

      // 禁用按鈕避免連點
      btn.disabled = true;
      btn.textContent = '命運醞釀中...';

      // 清空舊結果
      resultDiv.style.opacity = '0';
      sparkles.innerHTML = '';
      sparkles.style.opacity = '0';

      // 隨機挑一個
      const chosen = wishes[Math.floor(Math.random() * wishes.length)];

      // 3秒後顯示結果 + 特效
      setTimeout(() => {
        resultDiv.textContent = chosen;
        resultDiv.style.opacity = '1';
        resultDiv.style.transform = 'scale(1.3)';
        setTimeout(() => resultDiv.style.transform = 'scale(1)', 300);

        // 愛心爆炸特效
        const hearts = ['♥','💖','💕','💗','💝','🌸','✨','⭐'];
        let html = '';
        for(let i = 0; i < 30; i++) {
          const heart = hearts[Math.floor(Math.random()*hearts.length)];
          const delay = Math.random()*0.8;
          const left = Math.random()*100;
          html += `<span style="position:absolute; left:${left}%; animation: float 2s ${delay}s forwards; opacity:0;">${heart}</span>`;
        }
        sparkles.innerHTML = html;
        sparkles.style.opacity = '1';

        // 恢復按鈕
        btn.disabled = false;
        btn.textContent = '再決定一次！♥';
      }, 1500);
    });

    // 浮動動畫
    const style = document.createElement('style');
    style.textContent = `
      @keyframes float {
        0% { transform: translateY(0) scale(0); opacity: 0; }
        50% { opacity: 1; }
        100% { transform: translateY(-300px) scale(1.5); opacity: 0; }
      }
    `;
    document.head.appendChild(style);
  </script>
</body>
</html>