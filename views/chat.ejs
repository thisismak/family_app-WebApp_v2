<!DOCTYPE html>
<html lang="zh-TW">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="/css/style.css" rel="stylesheet">
  <%- include('partials/pwa') %>
    <title>家庭AI助手 - 我們的家庭空間</title>
    <style>
      body,
      html {
        height: 100%;
        margin: 0;
        background: #f8f9fa;
      }

      .chat-wrapper {
        height: 100vh;
        padding-top: 20px;
        padding-bottom: 20px;
        display: flex;
        flex-direction: column;
      }

      .header-bar {
        background: #007bff;
        color: white;
        padding: 1rem;
        border-radius: 0.5rem 0.5rem 0 0;
        box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
      }

      iframe {
        flex: 1;
        border: none;
        border-radius: 0 0 0.5rem 0.5rem;
        box-shadow: 0 4px 15px rgba(0, 0, 0, 0.1);
      }
    </style>
</head>

<body>
  <div class="container-fluid chat-wrapper">
    <div class="header-bar d-flex justify-content-between align-items-center">
      <h4 class="mb-0">家庭AI助手</h4>
      <a href="/dashboard" class="btn btn-light btn-sm">返回儀表板</a>
    </div>
    <!-- chat.ejs 直接改成這一行 -->
    <iframe src="/chatbot-frame" style="width: 100%; height: 100%; border: none;" allowfullscreen>
    </iframe>
  </div>
</body>

</html>