<!DOCTYPE html>
<html lang="zh-TW">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="/css/style.css" rel="stylesheet">
  <%- include('partials/pwa') %>
  <title>忘記密碼 - 我們的家庭空間</title>
</head>
<body>
  <div class="container mt-5 login">
    <h2 class="text-center mb-5">忘記密碼？</h2>
    <% if (error) { %>
      <div class="alert alert-danger"><%= error %></div>
    <% } %>
    <form action="/forgot-password" method="POST" class="card p-4">
      <div class="mb-3">
        <label for="recoveryCode" class="form-label text-info">請輸入恢復碼</label>
        <input type="text" id="recoveryCode" name="recoveryCode" class="form-control" 
               placeholder="例如：family123" required autofocus>
        <div class="form-text">這是你註冊時設定的固定代碼</div>
      </div>
      <button type="submit" class="btn btn-primary w-100">驗證並重置密碼</button>
    </form>
    <div class="text-center mt-3">
      <a href="/login" class="text-info">返回登入</a>
    </div>
  </div>
</body>
</html>